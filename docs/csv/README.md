# Local CSV data

Drop large facility datasets here for **local development only** (e.g. `VF_Hackathon_Dataset_India_Large.csv`).

Files in this directory matching `*.csv` are **gitignored** to avoid committing multi‑MB data. Copy from the hackathon / Databricks share as needed.
